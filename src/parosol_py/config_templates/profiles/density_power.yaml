# Override snippet for continuous density images with a power-law E mapping.
input:
  image_type: density

materials:
  units: MPa
  density:
    equation: power
    coefficient: 10000
    exponent: 1.7
    reference_density: 1000
    mask_threshold: 0
  poisson_ratio: 0.3
