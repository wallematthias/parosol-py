# Override snippet for large batch runs where compact JSON is the main output.
solver_profile: batch
output_profile: quick_summary

solver:
  mpi_processes: 6
  tolerance: 1.0e-6

output:
  export_fields: false
