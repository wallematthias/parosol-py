# Override snippet for z-axis bending with opposing top and bottom plate tilt.
solver_profile: batch
output_profile: quick_summary

load_case:
  type: bending
  axis: z
  bending_angle_degrees: 1
  neutral_axis_angle_degrees: 90
  center: center_of_mass

solver:
  outputs: [sed]
