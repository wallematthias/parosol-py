# Catch-all z-axis bone compression profile for uneven anatomy.
solver_profile: batch
output_profile: standard_fields

load_case:
  type: constrained_axial
  axis: z
  strain: -0.01
  surface:
    mode: smart
    depth: auto

solver:
  outputs: [sed]

output:
  export_fields: true
