# Override snippet for z-axis torsion with a rotated top plate.
solver_profile: batch
output_profile: quick_summary

load_case:
  type: torsion
  axis: z
  twist_angle_degrees: 1
  center: center_of_mass

solver:
  outputs: [sed]
